@model AbsenceViewModel
@inject UserManager<Employee> UserManager

@{
    ViewData["Title"] = "Lista urlopów";
}

<h1>Lista wszystkich dostępnych urlopów</h1>
<hr />

<div>
    <a class="btn btn-primary" style="width:auto" asp-controller="AvailableAbsence" asp-action="CreateAvailableAbsence">
        Dodaj urlop
    </a>
    <a class="btn btn-primary" style="width:auto" asp-controller="" asp-action="">
        Dodaj rodzaj urlopu
    </a>
</div>

<br />

@if (Model.AvailableAbsence.Any())
{
    <table class="table">
        <thead>
            <tr>
                <th>
                    Pracownik
                </th>
                <th>
                    Typ urlopu
                </th>
                <th>
                    Dostępne dni
                </th>
                <th>
                    Wykorzystane dni
                </th>
                <th>
                    Numer karty pracownika
                </th>
                <th>
                    Akcja
                </th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model.AvailableAbsence)
            {
                <tr>
                    <td>
                        @UserManager.FindByIdAsync(item.IdEmployee).Result.FirstName @UserManager.FindByIdAsync(item.IdEmployee).Result.LastName
                    </td>
                    <td>
                        @Model.AbsenceType.Find(a => a.IdAbsenceType == item.IdAbsenceType).AbsenceTypeName
                    </td>
                    <td>
                        @item.AvailableDays
                    </td>
                    <td>
                        @item.UsedAbsence
                    </td>
                    <td>
                        @UserManager.FindByIdAsync(item.IdEmployee).Result.IdCardNumber
                    </td>
                    <td>
                        <form method="post" asp-action="DeleteAvailableAbsence" asp-route-id="@item.IdAvailableAbsence">
                            <a class="btn btn-primary" style="width:auto" asp-controller="AvailableAbsence" asp-action="EditAvailableAbsence" asp-route-id="@item.IdAvailableAbsence">
                                Edytuj
                            </a>
                            <button type="submit" class="btn btn-danger">
                                Usuń
                            </button>
                        </form>
                    </td>
                </tr>
            }
        </tbody>
    </table>

}
else
{
    <h3>Brak dostępnych urlopów</h3>
}